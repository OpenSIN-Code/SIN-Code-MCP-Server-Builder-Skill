# {{ name }}

> {{ description }}

MCP server (Go) generated by `sin-mcp-server-builder`.

Tools: {{ tools | join(', ') }}

## Build

```bash
go mod tidy
go build -o bin/sin-{{ slug }}-mcp
```

## Run

```bash
./bin/sin-{{ slug }}-mcp
```
