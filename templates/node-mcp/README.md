# {{ name }}

> {{ description }}

MCP server (Node.js) generated by `sin-mcp-server-builder`.

Tools: {{ tools | join(', ') }}

## Install

```bash
npm install
```

## Run

```bash
npm start
# or
npx sin-{{ slug }}-mcp
```

## Register in opencode.json

```json
{
  "mcp": {
    "sin-{{ slug }}": {
      "type": "local",
      "command": ["npx", "-y", "sin-{{ slug }}-mcp"],
      "enabled": true
    }
  }
}
```
