# `tests/tools.test.js`

## What it does
Smoke tests for every tool exposed by `src/index.js`. Uses Node.js's
built-in test runner (`node --test`).

## Dependencies
- Node.js >= 18 (built-in `node:test`)

## Usage
```bash
npm test
```
