# `tests/test_tool_adder.py`

## What it does
Unit tests for the `ToolAdder` and `ToolSpec` dataclass.

## Dependencies
- `pytest`
- `sin_mcp_server_builder.tool_adder`

## Usage
```bash
pytest tests/test_tool_adder.py -q
```
