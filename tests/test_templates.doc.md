# `tests/test_templates.py`

## What it does
Unit tests for `sin_mcp_server_builder.templates`. Covers the registry
metadata, the `TemplateEngine` API, and the module-level helper.

## Dependencies
- `pytest`
- `sin_mcp_server_builder.templates`

## Usage
```bash
pytest tests/test_templates.py -q
```
